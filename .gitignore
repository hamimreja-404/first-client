node_modules

# Environment files — NEVER commit real secrets
.env
.env.production
.env.local
.env.*.local

# .env.example is safe to commit (contains no real secrets)
# .env.example

# Build output
dist
